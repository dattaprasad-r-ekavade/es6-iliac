---
description: Ratna Bay architecture and change boundaries for AI agents
alwaysApply: true
---

# Ratna Bay

- Game rules go in `src/RatnaBay.Domain`. Never add MonoGame types there.
- `Game1` is the lifecycle coordinator. New screens, HUD, and overlays go in `src/RatnaBay.Game/Ui/`.
- Sample input only through `InputRouter`. Draw 2D UI only through `UiCanvas`.
- Hit-test and draw rectangles share `UiLayout`. Do not duplicate pixel numbers in `Game1` and a renderer.
- Colours come from `UiTheme` by role. Don't write `new Color(...)` in a renderer for a colour the theme names.
- 3D boxes, the crystal and carved faces go through `SceneRenderer`. Imported props go through `ModelCache`.
- World-anchored flat interface (nameplates, floating damage, signs) goes through `WorldProjector` + `MarkerRenderer`.
- Do not revive `ParkedFeatures` or add a package without a focused spike.
- Validate with `.\verify.ps1` on Windows, or `dotnet test tests/RatnaBay.Domain.Tests` plus a Windows-targeted compile on Linux.
- To assert on a running client, drive it with a console script: `build\RatnaBay.exe --script Docs\scripts\smoke.rbs`. A script opens in the yard; `wait` is in simulated seconds; a failed `assert` exits non-zero.
- Full contributor recipes live in `AGENTS.md`.
